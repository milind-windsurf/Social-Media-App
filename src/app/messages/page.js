import { MessagesPage } from '@/components/MessagesPage';

/**
 * Messages page route component
 * @returns {React.ReactElement} Messages page component
 */
export default function Messages() {
  return <MessagesPage />;
}
